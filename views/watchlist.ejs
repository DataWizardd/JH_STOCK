<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>관심 종목</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <style>
    body {
      padding: 20px;
    }
    h1 {
      margin-bottom: 30px;
    }
    .stock-item {
      margin-bottom: 15px;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>관심 종목</h1>
    <ul class="list-group">
      <% watchlist.forEach(stock => { %>
        <li class="list-group-item stock-item">
          <div class="d-flex justify-content-between align-items-center">
            <div>
              <span><%= stock.symbol %></span> - 
              <span>가격: <%= stock.price %></span> - 
              <span>거래량: <%= stock.volume %></span> - 
              <span>시간: <%= stock.timestamp %></span>
            </div>
            <form action="/watchlist/delete/<%= stock.id %>" method="post">
              <button type="submit" class="btn btn-danger">삭제</button>
            </form>
          </div>
        </li>
      <% }); %>
    </ul>

    <form action="/watchlist/add" method="post" class="mt-3">
      <div class="form-group">
        <label for="symbol">Symbol:</label>
        <input type="text" id="symbol" name="symbol" class="form-control" required>
      </div>
      <button type="submit" class="btn btn-primary">추가</button>
    </form>
  </div>
</body>
</html>
