<!-- stock.ejs -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stock Price</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>주식 시세 조회</h1>
    <div class="container">
        <div class="section">
            <h2>주식 시세</h2>
            <table>
                <thead>
                    <tr>
                        <th>주식 심볼</th>
                        <th>주식 가격</th>
                        <th>거래량</th>
                        <th>가격 기록 시간</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (stocks && stocks.length > 0) { %>
                        <% stocks.forEach(stock => { %>
                            <tr>
                                <td><%= stock.symbol %></td>
                                <td><%= stock.price %></td>
                                <td><%= stock.volume %></td>
                                <td><%= stock.timestamp %></td>
                            </tr>
                        <% }); %>
                    <% } else { %>
                        <tr>
                            <td colspan="4">주식 시세가 없습니다.</td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>
